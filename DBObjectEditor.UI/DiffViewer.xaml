<Window x:Class="DBObjectEditor.UI.DiffViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Script Değişiklik Onayı (Toplu Görünüm)" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#F0F0F0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <TabControl Name="tabSpler" Grid.Row="0" Margin="10">
        </TabControl>

        <Border Grid.Row="1" Background="White" BorderBrush="LightGray" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="Tümünü İptal Et" Width="150" Height="40" Margin="0,0,20,0" Background="LightCoral" Foreground="White" FontWeight="Bold" Click="BtnIptal_Click"/>
                <Button Content="Tümünü Onayla ve Oracle'a Yaz" Width="250" Height="40" Background="LightGreen" FontWeight="Bold" Click="BtnOnay_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>